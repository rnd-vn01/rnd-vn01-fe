import './QuizTimer.scss';
import React, { useState } from 'react';

export const QuizTimer: React.FC<IQuizTimer> = ({ }) => {
  return (
    <div
      role="div"
      aria-label="quiz-timer"
      className="quiz-timer">

    </div>
  );
};
